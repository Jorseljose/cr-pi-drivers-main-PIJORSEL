import style from "../Create/Create.module.css";

const Create = ({
  image,
  name,
  lasName,
  nationality,
  description,
  birthDate,
  teams,
}) => {
  return <div></div>;
};

export default Create;
